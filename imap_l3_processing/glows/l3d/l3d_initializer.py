from imap_l3_processing.glows.l3d.glows_l3d_dependencies import GlowsL3DDependencies


def setup_and_run_toolkit(l3d_dependencies: GlowsL3DDependencies):
    # open up l3d file and determine which CR we need to process
    # query for l3b and l3c files
    # filter down list of files to find the nearest 1 and the next CR thereafter
    # validate that we have sufficient l3b/c files to process the next CR
    # get l3a parents off l3b files
    # download all l3 files
    # write all CDFs to json files
    # invoke Izabela's code
    pass
